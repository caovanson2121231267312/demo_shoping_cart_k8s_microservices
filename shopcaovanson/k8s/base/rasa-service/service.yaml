apiVersion: v1
kind: Service
metadata:
  name: rasa-service
  namespace: shop
  labels:
    app: rasa-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    name: http
  selector:
    app: rasa-service
