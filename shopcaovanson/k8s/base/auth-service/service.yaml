apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: shop
  labels:
    app: auth-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    name: http
  selector:
    app: auth-service
