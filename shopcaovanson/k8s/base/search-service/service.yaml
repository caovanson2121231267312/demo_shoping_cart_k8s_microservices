apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: shop
  labels:
    app: search-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    name: http
  selector:
    app: search-service
