apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: shop
  labels:
    app: analytics-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    name: http
  selector:
    app: analytics-service
