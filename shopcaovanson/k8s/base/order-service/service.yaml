apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: shop
  labels:
    app: order-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    name: http
  selector:
    app: order-service
